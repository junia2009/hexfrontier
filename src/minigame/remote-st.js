// 散策部屋で配る「見た目の状態」。
//
// サーバー(server/walk-relay.js)とクライアント(remote.js / walk-mode.js)の
// 両方が使うので、どちらからも読める src/ 側に1つだけ置く
// ── 2か所に書くと、片方だけ増やしたときに静かにずれる。
//
// 姿勢を全部送るのは重いので、「どの姿勢を使うか」だけを数字で送り、
// 細かい角度は受け取った側が自分で作る。

// 散策部屋の席数。サーバー(部屋の席)・画面(名簿と案内文)・色の数が
// これで揃う。3か所に別々に書くと、席を増やしたときに静かにずれる。
export const WALK_SEATS = 8;

export const ST = {
  walk: 0,   // 立っている・歩いている
  air: 1,    // 跳んでいる
  fall: 2,   // 海へ落ちている・沈んでいる
  fish: 3,   // 釣っている
  sit: 4,    // 円卓に座っている
};

export const ST_MAX = 4;

// エモートの番号の上限。0 は「出していない」。
// 中身(絵柄・名前・長さ)はクライアントだけが知っていればよいので emote.js。
// サーバーは範囲を丸めるためにこの数だけ知る。
export const EMOTE_MAX = 5;
